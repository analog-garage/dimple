This directory contains general tests that are neither
specific to a particular algorithm (in which case it should go in one of
the algo*/ directories) nor is it common to all algorithms (in which case
it should go in core/).

